# DeepSeek 本地桌面客户端 — 需求说明

## 一、项目背景
将 DeepSeek 网页版（`chat.deepseek.com`）的交互完整迁移到一个独立的本地桌面程序界面中。
核心目标：**网页返回什么，本地程序就显示什么；网页能访问的 DeepSeek 界面，本地程序也必须支持。**

## 二、核心需求
1. **原生窗口承载网页**：本地程序以独立桌面窗口运行 `chat.deepseek.com` 本身（而非逆向重写一套客户端），保证内容与网页 100% 一致。
2. **登录一致性**：使用与网页完全相同的「用户名 + 密码」登录流程，复用 DeepSeek 网页登录（含 Cloudflare 校验）。
3. **分享对话直达**：程序启动自动打开指定分享对话（`b2bd094e-6d56-4d44-a5a0-910c2aaa5d70`），并提供一键回到该链接的入口。

## 三、本地增强功能（浮动工具条）
在网页上下文中注入本地浮动工具条：
- **打开分享对话**：一键跳回指定分享链接。
- **保存到本地**：将当前对话导出为 Markdown，存至本地 `saved_conversations/`。

## 四、本地工作目录访问（双向读写）
- **能力范围**：本地程序可读取用户选定文件夹中的文件作为上下文喂给 DeepSeek，也可将 DeepSeek 回复写回该文件夹。
- **目录指定**：每次通过系统文件夹选择框手动选定，**不写死路径**。
- **读取 → 对话**：将文本文件（代码 / 文档 / md / json / csv 等，自动跳过二进制）拼成带 `===== 文件: xxx =====` 标记的上下文填入输入框；若网页改版导致填不入，降级为复制到剪贴板手动粘贴。
- **写回目录**：用户粘贴回复并指定文件名，由本地程序写回所选文件夹。
- **导出当前页**：支持把整页对话存成 md（进工作目录或本地备份目录）。
- **安全边界**：DeepSeek 服务器不接触用户磁盘，所有读写由本地程序代理；选中目录仅存于本地 `workspace.json`（不进仓库）。

## 五、运行与交付
- 形态：Python + pywebview 本地桌面窗口程序，文件含 `app.py` / `requirements.txt` / `run.bat`。
- 仓库：GitHub 私有仓库 `zhangshifa/deepseek_desktop`。
- 本机运行：`pip install -r requirements.txt` 后 `python app.py`（需 Windows WebView2 运行时）。
